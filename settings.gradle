include ':app'
rootProject.name = "activity-tracker"

includeBuild('../ComposeReorderable') {
    dependencySubstitution {
        substitute module('org.burnoutcrew.composereorderable:reorderable') using project(':reorderable')
    }
}